apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sonarqube
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: default
  source:
    repoURL: https://github.com/muchiny/media-stack-k8s.git
    targetRevision: HEAD
    path: charts/sonarqube
    helm:
      releaseName: sonarqube
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    # Namespace dedie : SonarQube n'est pas de la stack media et ne doit pas
    # consommer le ResourceQuota ni les PriorityClass de media-stack.
    namespace: sonarqube
  # Le serveur API renseigne apiVersion/kind/volumeMode/status dans les
  # volumeClaimTemplates du StatefulSet : sans cela l'app reste OutOfSync en
  # permanence et selfHeal reapplique le manifeste en boucle.
  ignoreDifferences:
    - group: apps
      kind: StatefulSet
      name: sonarqube-postgres
      namespace: sonarqube
      jsonPointers:
        - /spec/volumeClaimTemplates
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
